# ──────────────────────────────────────────────────────────────
# requirements.txt
# IA Educativa — dependencias sin Firebase
# ──────────────────────────────────────────────────────────────

# Web framework
Flask==2.3.3

# Peticiones HTTP (YouTube scraping, etc.)
requests>=2.25.0,<3.0

# Seguridad de contraseñas (ya viene con Flask/Werkzeug, se declara explícitamente)
Werkzeug>=2.3.0

# Google Gemini AI
google-generativeai>=0.3.2

# Carga de variables de entorno desde .env
python-dotenv>=1.0.0

# Herramientas de construcción
setuptools>=65.0